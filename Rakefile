require "bundler/gem_tasks"
require "rspec/core/rake_task"

RSpec::Core::RakeTask.new(:spec)

task :default => :spec

desc "Scrape emails of all comitters of all repositories from a GitHub handle"
task :emails do
    
end